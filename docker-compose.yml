version: '2'
services:
  db:
    image: mysql
    #build: .
    container_name: www-mysqldb
    environment:
      MYSQL_DATABASE: login
      MYSQL_USER: cockpit
      MYSQL_PASSWORD: x1ugOm71CraidpNs
      MYSQL_ROOT_PASSWORD: x1ugOm71CraidpNs
    volumes:
      - ./mysql/:/var/lib/mysql/
    restart: always
    ports:
      - 3306:3306
  cache:
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning
    #  --requirepass eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81
    volumes: 
      - cache:/data
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root

  # mongo-express:
  #   image: mongo-express
  #   restart: always
  #   ports:
  #     - 8081:8081
  #   environment:
  #     ME_CONFIG_MONGODB_ADMINUSERNAME: root
  #     ME_CONFIG_MONGODB_ADMINPASSWORD: example
  #     ME_CONFIG_MONGODB_URL: mongodb://root:example@mongo:27017/
volumes:
  cache:
    driver: local